# spec/features/user_views_tasks_in_project_spec.rb

require 'rails_helper'

RSpec.describe 'User views tasks in a project', type: :feature do
  # let(:user) { create(:user) }
  # let(:project) { create(:project) }
  # let!(:task) { create(:task, project: project, author: user, assignee: user) }

  # before do
  #   create(:user_project, user: user, project: project)
  #   login_as(user)
  # end

  # scenario 'User sees all tasks in a project they have access to' do
  #   visit project_tasks_path(project)

  #   expect(page).to have_content(task.title)
  #   expect(page).to have_content(task.description)
  # end
end
